# INTEGRATED-02 复核记录

本次以Chrome实际浏览器验证：

- 900/1000/1100/1280/1440/1920px语言面板与两侧圆角坐标一致；1000px搜索连接几何正确。
- 1920/390px六语言导航、Hero、Catalog同步；当前产品不变；中文产品可搜索；刷新记忆；晚注册板块立即收到当前语言。
- 搜索清空保持打开；鼠标关闭后移出导航，顶部恢复透明。
- 两种主尺寸导航到Catalog、入场完成、产品选择正常，无横向溢出、破图或脚本错误。
- 新区块h1及自有data-copy不会被Hero语言更新误改。
- 新注册搜索源按当前语言提供数据；联系目标注册后Catalog CTA平滑滚动到真实目标。测试节点仅在测试浏览器临时存在。

资源清理：经SHA-256确认，桌面/手机产品PNG、重复Roboto、重复品牌标志均为完全相同字节，已复用共用文件；未重新压缩或改变图像质量。旧Hero延续背景CSS、旧下拉硬编码定位及Catalog全局基础样式已移除。

未来Section尚未实现，仍需按DEVELOPMENT.md执行整页验收。本次没有宣称完成所有浏览器、200%文字放大或全部平板的完整验收。

## INTEGRATED-03 · About Us · 2026-09-17

- 读取 Figma Desktop 211:134 与 Mobile 213:126 的 design context，并对比实际截图。1920px/390px下媒体、信息卡、标题、CTA坐标和尺寸符合读取的设计值；英文正常内容分别约984px/650px高。
- 320、390、768、900、1100、1440、1920宽度测试：无稳定态横向页面溢出、素材加载失败或脚本错误；六语言注册和搜索索引正常。长译文自然增加卡片高度。
- 桌面及手机实际检查首次入场的delay/duration；完成后滚出再滚回不重播；减少动态效果直接显示。移动200%文字检查通过。切换系统动态偏好并缩放时，需待现有Hero过渡帧完成后再测量静态溢出；本次未改Hero动画。
- 使用本地临时生成视频验证静音、loop、playsInline、无controls、poster、播放及减少动态效果暂停/恢复；测试素材不随交付。默认交付仍为Figma图片。无效视频地址回退保留建筑poster。
- 图片/视频使用相同容器；替换不会改变布局。Figma桌面图、手机图与既有Hero建筑图SHA256相同，复用已有资源；仅新增两枚原始SVG功能图标。
- About CTA通过临时注册的真实DOM目标验证平滑滚动；移除测试目标后恢复待接入反馈。未伪造Contact Us或产品详情。
- 回归combined、hooks、global检查：1920/390 Catalog选择、全局语言、查询清空、关闭后透明、目标注册，以及900–1920下拉圆角连接全部通过。
- 本次未改变产品数据、卡片Morph、Carousel及Hero视觉。Catalog入场的观察和生命周期抽为共享motion.js；原时间表和完成事件保留。

交付边界：五种非英文语言为预览译稿；正式视频和Contact Us尚未提供。布局与交互已完成，最终视觉仍待用户确认。

### Catalog 图片完整显示回归

连续切换1920、1440、1100、900、899、768、600、390、375、320，再返回768/1920：图片加载正常、使用contain、图片容器始终位于卡片内，无页面横向溢出，当前产品03保留。实际截图复核390/768的产品顶部、底座完整可见。仅修改图像适配与紧凑布局图片高度规则，未改卡片选择、变形或滑动逻辑。

### About Us 桌面对齐回归

1920/1600/1440/1366/1280/1024/900和1920→900每17px一次的61个连续采样：首卡与媒体顶部差小于0.1px，媒体保持972/571.5比例，无横向溢出。覆盖入场前缩放、入场中缩放、完成后缩放；完成状态正常。截图复核1920/1024；390px媒体、两卡、分隔线、CTA坐标与修改前一致。

### About Us 等高联动回归（取代仅顶部对齐的验收）

1920、1600、1440、1366、1280、1100、1024、950、900 × 六语言：左列与媒体高度差/顶部差均为0，正文底部位于卡片内。1920两卡约276.04px，900约172.26px，正文最小12px。61个连续宽度采样及入场前/中/后缩放通过；390px原位置不变。真实临时视频模式检查同一组桌面宽度下等高关系、媒体播放/减少动态效果暂停恢复，图片视频使用同一frame。

## 保守清理回归 · 2026-09-17

见CLEANUP.md：仅删除一条无匹配选择器分支及9项共用CTA重复声明。44组视觉基准对比（43组像素完全相同，1组栅格噪声经全部计算样式和几何检查确认一致）、整页交互、连续缩放、媒体及减少动态效果检查通过。原有功能与视觉参数保留。


## INTEGRATED-04 Trusted Feedback · 2026-09-17

- 已读取Figma Desktop211:246及Mobile213:184，实际浏览器截图检查1920/390。390主要位置：地图y69、标题y322、媒体y375高243、反馈y637、分隔线y687、CTA y706；CTA按本次要求居中。
- 检查1920/1600/1440/1366/1280/1024/900/899/768/430/390/375/360；另外从1920到360每31px连续改变浏览器视口，未发现横向滚动。此为自动化连续resize，不是手工拖拽。
- 每个检查宽度临时增加标题50%、反馈100%，分隔线保持位于反馈下方，无横向溢出，测试后恢复。
- 真实生成视频验证Trusted及About视频播放、静音、inline、减弱动态暂停/恢复，以及200%手机文字检查。
- Trusted六语言更新、CTA注册真实测试目标后的滚动、入场一次且返回不重播通过。
- Catalog/About在1920/390与加入Trusted前快照的文字、元素尺寸、字体、margin、padding一致；原产品真实触摸滑动、两阶段点击、键盘、分页、搜索清空及导航关闭测试通过。
- 新板块运行检查无pageerror，全部图片加载完成。截图已人工查看；不声称整页逐像素完全一致。
- 当前为纯静态HTML/JS，无框架build/typecheck/lint配置；所有JS通过node语法检查。无新增依赖。
- Contact Us仍未实现；当前CTA保持既有提示和接入钩子。翻译为预览译稿。


## INTEGRATED-05 Contact Us

- Figma读取：Desktop211:330、Mobile214:94、Tablet215:2；1920/390截图已查看。两套主截图保存在本目录。
- 响应式：1920/1600/1440/1366/1280/1024/900/899/768/600/599/430/390/375/360，无横向溢出；自动化每39px连续改变视口复核，不声称人工拖拽。
- 空提交识别7个必填项，Phone不报错，首个错误获焦。错误输入与修正、邮箱校验、249地区、uni/sing/can/jap/ger查询、无匹配、键盘选择、Business选择、Type other及返回、空格无效、预览提交均通过。
- 长Message超过可视高度5倍，外框高度不变；触摸模拟和光标跟随单独测试。
- 六语言、标题翻倍/长标签、全部错误显示、列表内滚动、互斥打开、Escape、Tab、390x380短视口、单次入场通过。真实iOS/Android系统键盘未实机测试。
- 前板块CTA滚动到真实contact；表单仅前端预览，未发送任何生产提交请求。
- Catalog/About/Trusted在1920/390与接入前快照的内容、元素尺寸、字体、margin、padding一致；导航搜索清空/关闭复核通过。未声称所有状态逐像素相等。
- 浏览器表单测试无pageerror；全部JS语法检查通过。无新增依赖，无项目build/typecheck/lint配置。
- 非英文译文为预览译稿；Email/WhatsApp为Figma显示文本，真实性未确认，未生成生产联系方式链接。

测试脚本依赖本机playwright-core及Chrome，保存在交付运行目录之外；无测试内容进入页面。


### Contact focus 局部修正

1920/390实际逐项点击8个字段，确认焦点outline/shadow为none、背景与尺寸不变；Type other输入、必填错误红框、首错聚焦、Region方向键与Enter选择通过。仅改Contact CSS，未改JS交互或全局CSS。


### Business type Type other 增量验证

1920/390实际验证7个选项顺序、末项Type other、同一input自动获焦、无焦点outline/shadow、输入后校验解除、空格必填报错、箭头返回Wholesaler、两个自定义字段值独立、最终submit handler接收trim后的businessType文字。Region仍为249项且无Type other。未修改CSS/布局/动画。


# Responsive Content Safety QA · 2026-09-18

实际修改：Contact字段按内容所需宽度自动重排、文字与箭头安全间隔；CTA长文字换行；About长文案参与共享行高度；Catalog长文字换行；窄桌面Hero卡片宽度受标题区域约束。

已完成：
- 1920/1728/1600/1536/1440/1366/1280/1200/1024/900/834/768/600/430/390/375/360六语言提示与按钮区域检查。
- 从1920到904每8px连续改变浏览器视口，共128个宽度，无Region提示截断或页面横向溢出。为自动化连续resize，非人工拖拽。
- 标题增加约50%、描述增加100%、CTA文字加长，卡片内容边界及About等高检查通过，测试后恢复原文。
- 横图、竖图替换About/Trusted，媒体框尺寸稳定；原16:9视频、减弱动态暂停/恢复及About等高回归通过。
- 产品图片contain全尺寸检查、Business Type自定义/返回/提交值、Focus视觉、键盘、搜索清空与关闭通过。
- Hero900px重叠已消除，1920/1280/1024/768/390/360保持不重叠。
- 1920/390截图生成并查看Contact1920、About390。没有声称所有截图逐像素相同。
- 运行时JS与修改前逐文件字节一致，全部JS语法通过；无新增框架或依赖。

边界：真实移动系统键盘及多浏览器未实机覆盖。原生单行输入的任意长自定义文字继续内部水平滚动；不人为省略，箭头区域独立。About超长文案超过原比例基准时共同增高，媒体cover保持不变形，正常文案仍保持原比例。


## INTEGRATED-06 Footer（2026-09-18）

- 读取Figma Desktop211:590 / Mobile215:110，检查1920/390截图；整合Footer替换底部测试留白。
- 17个尺寸检查（1920、1728、1600、1536、1440、1366、1280、1200、1024、900、899、834、768、430、390、375、360）：Footer无横向溢出、可见文字无容器裁切。
- 自动连续Resize：1920→360，每8px一步，共196个尺寸；没有新增响应式补丁断点。
- 六语言×五尺寸共30组合通过；长Heading、邮箱、号码、菜单文案通过。测试仅改浏览器DOM，正式文案已恢复。
- 原有Hero/Catalog/About/Trusted/Contact在10个尺寸的top/width/height与加入Footer前快照完全一致。
- Header/Footer到Catalog/About/Contact最终scrollY一致；Footer返回顶部为0；Email为mailto，WhatsApp无链接。
- 首次入场完成后离开再返回不重播；Reduced Motion可直接显示；真实生成视频播放、muted/loop/playsInline、无controls、减少动态效果暂停、断点切换不重建视频均通过。
- 既有Contact Type other、验证与预览提交、249地区选项、搜索关闭、产品图片contain回归通过。
- 无浏览器运行异常和失败资源请求；纯静态项目无build/typecheck/lint配置，使用现有浏览器回归与JS语法检查。
- 证据与脚本：`../footer-qa-2026-09-18/`。此处连续Resize指浏览器自动化，不是人工拖动窗口。


## INTEGRATED-07 全局流式字体与CTA

已执行：
- 六种现有语言 × 16个宽度，共96组合：1920、1728、1600、1536、1440、1366、1280、1200、1100、1024、834、768、430、390、375、360。
- 英文与加长英文分别自动连续Resize：1920→1024，每8px一步，共226次检查。此为浏览器自动化，非人工拖动。
- 德语临时文案在1920/1440/1280/1024/390/360下压力测试；未保存为正式翻译。
- CTA完整、无图标与文字重叠；法语Contactez-nous在检查尺寸中保持单行；未发现运行异常。
- 法语Section CTA实测：1920为约233×58.5px /18px字；1440为约186×44px /15px字；1024为约172×44px /15px字；390为约130×28px /12px字。
- 全页content-safety脚本完成，未报告卡片、表单或文本边界失败。
- Contact Type other、地区249项、验证/预览提交、搜索清除关闭、产品真实触屏滑动、分页、两阶段点击与键盘操作回归通过。
- 所有行为JS与改动前逐文件校验；只有content.js中Search展示文案变化，行为脚本无改动。
- 页面Section顺序、媒体资源、产品/表单/导航/动画逻辑保留。字体和CTA尺寸按本任务响应式规则改变；不声称所有宽度像素不变。

截图与专项脚本见本目录。当前纯静态项目没有build/typecheck/lint配置。

证据：`../fluid-qa-2026-09-19/`。
