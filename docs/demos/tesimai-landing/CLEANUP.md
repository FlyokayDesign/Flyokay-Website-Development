# Landing Page 保守清理报告 · 2026-09-17

基准：清理开始时的当前整合预览。实际Section顺序为Hero → Catalog → About Us，外加全局导航、语言、搜索、Menu及纯白测试留白。未重新解释Figma。

## 实际修改（SAFE）

仅两份运行时CSS有改动：
- style.css：删除不匹配任何当前元素且没有生成路径的旧选择器分支 `.menu-#desktop-nav button`；同组有效的 `.menu-nav button` 保留。
- catalog/catalog.css：删除移动CTA中9项重复声明（按钮宽高、字号、行高、padding、图标容器宽高、箭头宽高），由已加载的components.css中同值section-cta规则提供。Catalog自身定位和外边距保留。

没有修改任何JavaScript、HTML、素材、字体、断点、动画参数、产品内容或交互。没有新增依赖、抽象层或构建工具。

## 审查后保留（REVIEW / DO NOT TOUCH）

- Catalog/About共享motion.js已存在；轮播nudge观察器使用不同目标和0.55阈值，不与入场观察器合并。
- 导航下拉位置测量、Menu摄影区高度测量、轮播轨道宽度计算都有当前用途，保留。
- 静态页面的全局事件只在初始化注册；About提供卸载清理。未为理论性能收益重构全站生命周期。
- 当前CSS覆盖关系、图片/视频回退、轮播包装层、无障碍状态、测试留白均有作用，保留。
- 既有独立Hero/Catalog历史包不是当前运行入口，没有删除其资源。

## 验证

- 冻结副本和文件指纹已保存；cleanup.patch记录两份CSS的可逆差异。
- 11种宽度：1920、1600、1440、1366、1280、1024、900、768、390、375、360；每种页面/语言/Search/产品03状态，共44组截图。
- 43组像素完全相同。1600px Search组存在最大10/255的少量栅格颜色差异；进一步对所有元素的computed styles和几何尺寸逐项比对，完全相同。未发现布局或视觉行为回归。采集时等待字体/图片解码、实际滚动屏外图片，并隐藏闪烁输入光标。
- 12个JavaScript文件通过node --check。该静态包没有现成build/typecheck/lint脚本；未新增工具链。
- 控制台错误/警告、失效图片检查：11种宽度均为0。素材字节指纹扫描没有重复文件；未压缩、替换素材。
- 实际测试导航滚动、手机顶部透明、Menu锁滚与恢复、字标/控件对齐、六语言及搜索、无结果清空、关闭恢复透明。
- 真实触摸Swipe不触发详情；Tap在测试注入真实地址并拦截导航后产生正确事件；分页只选择；桌面小卡选中/大卡二次点击、键盘操作、缺省联系反馈及临时真实联系目标滚动通过。测试目标未写入交付页面。
- Hero两卡独立悬停、全页滚动、Catalog/About首次入场、About图标/媒体悬停通过。
- About六语言等高、61个连续宽度采样、入场前中后Resize、390px位置、真实临时视频及减少动态效果暂停/恢复通过。

性能检查为代码级与运行时合理性检查，不是FPS或Lighthouse基准。未发现需要本轮安全修改的重复执行、无限定时工作或重复素材文件，因此没有进行推测性的优化。
