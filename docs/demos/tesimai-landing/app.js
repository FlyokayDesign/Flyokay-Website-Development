(()=>{'use strict';const $=s=>document.querySelector(s),C=window.TESIMAI,header=$('#header'),desktop=$('#desktop-panel'),menu=$('#menu');let locale='EN';try{locale=localStorage.getItem('tesimai-language')||'EN'}catch{}if(!C.copy[locale])locale='EN';let panel=null,menuMode=null,query='',timer,leaveTimer,savedScroll=0,lastY=scrollY,accum=0,previousDirection=0;const mobile=()=>matchMedia('(max-width:899px)').matches;const t=()=>C.copy[locale];const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const closeMarkup=()=>`<button class="close" data-close-search aria-label="${t().close}"><i><img src="assets/close.svg" alt=""></i></button>`;
function links(){return t().nav.map((n,i)=>`<button data-target="${TESIMAI.navigation[i]}">${n}</button>`).join('')}
function render(){window.TESIMAI_I18N.setLanguage(locale);document.documentElement.lang=C.languages.find(l=>l[0]===locale)[2];$('#desktop-nav').innerHTML=links();document.querySelectorAll('#header [data-copy],#hero [data-copy]').forEach(n=>n.textContent=t()[n.dataset.copy]);document.querySelectorAll('#header .language span,#menu .language span').forEach(n=>n.textContent=locale);$('.hero-content h1').textContent=mobile()?t().heading:t().heading.toUpperCase();$('#cards').innerHTML=C.cards.map((c,i)=>`<article class="card" id="card-${i}" style="--media-height:${c.height}px;--mobile-media-height:${c.mobileHeight}px;--fit:${c.fit};--focus:${c.focus}"><div class="card-media"><img src="${c.image}" alt="${escape(t().titles[i])}"></div><div class="card-text"><h2>${escape(t().titles[i])}</h2><p>${escape(t().descriptions[i])}</p></div></article>`).join('');$('#menu-open').setAttribute('aria-label',t().nav[0]+' Menu');$('#menu-close').setAttribute('aria-label',t().close);menu.style.setProperty('--menu-height',$('.photo').getBoundingClientRect().height*.7+'px')}
function langHTML(){return `<div class="language-list">${C.languages.map(l=>`<button data-language="${l[0]}" lang="${l[2]}">${l[1]}</button>`).join('')}</div>`}
function searchHTML(inMenu){return `<div class="search-top">${inMenu?`<button data-back aria-label="${t().back}"><img src="assets/chevron.svg" alt=""><span>${t().back}</span></button><h2 class="mobile-search-title">${t().search.replace(/\.{3}$/,'')}</h2>`:`${closeMarkup()}`}</div><div class="search-field"><input type="search" aria-label="${escape(t().placeholder)}" placeholder="${escape(t().placeholder)}" value="${escape(query)}"><button data-clear aria-label="${t().clear}" ${query?'':'hidden'}>×</button></div><div class="results" aria-live="polite"></div>`}
function setDesktop(next,focus=false){clearTimeout(leaveTimer);panel=next;desktop.hidden=!next;desktop.className=next==='language'?'lang-panel':'search-panel';header.classList.toggle('open',!!next);$('#language').setAttribute('aria-expanded',next==='language');if(next){header.classList.remove('hide');desktop.innerHTML=next==='language'?langHTML():searchHTML(false);if(next==='search'){results(desktop);if(focus)desktop.querySelector('input').focus()}else if(focus)desktop.querySelector('button').focus()}else desktop.replaceChildren()}
function setMenu(mode){menuMode=mode;$('#mobile-language').setAttribute('aria-expanded',mode==='language');$('#menu-content').classList.toggle('is-search',mode==='search');$('#menu-content').innerHTML=mode==='language'?langHTML():mode==='search'?searchHTML(true):`<nav class="menu-nav">${links()}</nav><button class="pill menu-search" data-mobile-search><span>${t().search}</span><i><img src="assets/search.svg" alt=""></i></button>`;if(mode==='search'){results($('#menu-content'));$('#menu-content input').focus()}}
function openMenu(){setDesktop(null);savedScroll=scrollY;menu.hidden=false;document.documentElement.classList.add('locked');document.body.classList.add('locked');document.body.style.top=-savedScroll+'px';header.classList.remove('hide');$('#menu-open').setAttribute('aria-expanded','true');setMenu('nav');$('#menu-close').focus()}
function closeMenu(){if(menu.hidden)return;menu.hidden=true;menuMode=null;document.documentElement.classList.remove('locked');document.body.classList.remove('locked');document.body.style.top='';window.scrollTo({top:savedScroll,behavior:'instant'});lastY=savedScroll;$('#menu-open').setAttribute('aria-expanded','false');$('#menu-open').focus()}
function results(parent){const q=query.trim().toLocaleLowerCase();let html;if(!q)html=`<p>${t().prompt}</p><p>${t().hint}</p>`;else{const items=[{title:t().heading,desc:t().nav[0],target:'home'},...t().titles.map((title,i)=>({title,desc:t().descriptions[i],target:'card-'+i})),...window.TESIMAI_SITE.searchEntries()].filter(r=>(r.title+' '+r.desc).toLocaleLowerCase().includes(q));html=items.length?`<p>${t().result} · ${items.length}</p>`+items.map(r=>`<button class="result" data-result="${r.target}"><strong>${r.title}</strong><small>${escape(r.section||t().section)}</small><span>${r.desc}</span></button>`).join(''):`<p>${t().none}</p><p>${t().retry}</p><button class="clear-query" data-clear>${t().clear}</button>`}parent.querySelector('.results').innerHTML=html;parent.querySelector('[data-clear]').hidden=!query}
function toast(){const n=$('#toast');n.textContent=t().unavailable;n.hidden=false;clearTimeout(n._timer);n._timer=setTimeout(()=>n.hidden=true,3000)}
function target(id){closeMenu();setDesktop(null);if(id==='home'){window.scrollTo({top:0,behavior:matchMedia('(prefers-reduced-motion:reduce)').matches?'instant':'smooth'});return}if(window.TESIMAI_SITE.navigate(id))return;if(id.startsWith('card-')){const node=document.getElementById(id);const y=node.getBoundingClientRect().top+scrollY-125;window.scrollTo({top:Math.max(0,y),behavior:'smooth'});return}toast()}
$('#language').addEventListener('mouseenter',()=>{if(matchMedia('(hover:hover)').matches)setDesktop('language')});header.addEventListener('mouseleave',()=>{if(panel==='language')leaveTimer=setTimeout(()=>{if(!desktop.contains(document.activeElement))setDesktop(null)},180)});header.addEventListener('mouseenter',()=>clearTimeout(leaveTimer));$('#language').onclick=()=>setDesktop('language',true);$('#search-open').onclick=()=>setDesktop(panel==='search'?null:'search',true);$('#menu-open').onclick=openMenu;$('#menu-close').onclick=closeMenu;$('#mobile-language').onclick=()=>setMenu(menuMode==='language'?'nav':'language');
document.addEventListener('click',e=>{const b=e.target.closest('button');if(b?.dataset.language){locale=b.dataset.language;try{localStorage.setItem('tesimai-language',locale)}catch{}query='';clearTimeout(timer);render();setDesktop(null);if(!menu.hidden)setMenu('nav');else $('#language').focus();return}if(b?.dataset.target){target(b.dataset.target);return}if(b?.dataset.result){target(b.dataset.result);return}if(b?.hasAttribute('data-mobile-search'))setMenu('search');if(b?.hasAttribute('data-back')){setMenu('nav');$('#mobile-language').focus()}if(b?.hasAttribute('data-clear')){clearTimeout(timer);query='';const p=menu.hidden?desktop:$('#menu-content');p.querySelector('input').value='';results(p);p.querySelector('input').focus();return}if(b?.hasAttribute('data-close-search')){setDesktop(null);if(e.detail===0)$('#search-open').focus();else if(header.contains(document.activeElement))document.activeElement.blur();return}if(panel&&!header.contains(e.target))setDesktop(null)});
document.addEventListener('input',e=>{if(e.target.matches('input[type=search]')){query=e.target.value;clearTimeout(timer);const p=menu.hidden?desktop:$('#menu-content');timer=setTimeout(()=>{if(p.querySelector('.results'))results(p)},180)}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(!menu.hidden){if(menuMode!=='nav'){setMenu('nav');$('#mobile-language').focus()}else closeMenu()}else if(panel){const was=panel;setDesktop(null);$(was==='language'?'#language':'#search-open').focus()}}if(e.key==='Tab'&&!menu.hidden){const f=[...menu.querySelectorAll('button,input')].filter(n=>!n.hidden&&n.getClientRects().length);const first=f[0],last=f.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});
// rAF keeps scroll direction work out of the event hot path.
let scheduled=false;window.addEventListener('scroll',()=>{if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;if(!menu.hidden)return;const y=Math.max(0,scrollY),delta=y-lastY,dir=Math.sign(delta);if(dir!==previousDirection)accum=0;accum+=delta;previousDirection=dir;header.classList.toggle('solid',y>12);if(y<=12)header.classList.remove('hide');else if(!panel&&!header.contains(document.activeElement)){if(accum>12)header.classList.add('hide');if(accum< -8)header.classList.remove('hide')}lastY=y})},{passive:true});
let lastMobile=mobile();window.addEventListener('resize',()=>{menu.style.setProperty('--menu-height',$('.photo').getBoundingClientRect().height*.7+'px');if(lastMobile!==mobile()){closeMenu();setDesktop(null);lastMobile=mobile();$('.hero-content h1').textContent=mobile()?t().heading:t().heading.toUpperCase()}});render();window.TESIMAI_I18N.register('navigation-hero',next=>{if(next!==locale){locale=next;query='';clearTimeout(timer);render();setDesktop(null);if(!menu.hidden)setMenu('nav')}});window.TESIMAI_PREVIEW={setLanguage(code){if(C.copy[code]){locale=code;query='';render()}},refreshCards:render};})();
